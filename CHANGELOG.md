# 更新紀錄

本檔案記錄各版本的主要變動。格式參考 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，
版號採用 [語意化版本](https://semver.org/lang/zh-TW/)。

## [1.3.0] - 2026-08-19

這一版的重點是**修正內建調校演算法的錯誤建議**。
起因是實機出現速度反覆從 60 t/s 掉到 10 t/s 的狀況，追查後發現
程式原本給的建議值本身就是掉速主因。完整排查過程見
[`docs/benchmark-report.md`](docs/benchmark-report.md)。

### 修正

- **設定改完按「啟動」不會生效**：`StartServer()` 只更新記憶體中的設定，
  未寫回磁碟，導致下次開啟又變回舊值。現已在啟動時一併存檔。
- **context 建議值過大造成掉速**：24 GB 顯卡上限改為 32768。
  KV cache 在啟動時就全額配置，與實際用到多深無關；
  超過顯示卡的顯存臨界點後會外溢到主記憶體，速度直接砍到六分之一。
- **`--cache-ram` 建議值過大拖垮主記憶體**：改為扣掉模型大小後取一半，
  辦公情境再減半。原本在 32 GB 機器上建議 16384，
  實測造成 `Pages Input/sec` 破 1300、Modified page list 達 9.76 GB。
- **`--no-mmap` 不再預設開啟**：改為只在主記憶體達模型大小兩倍以上時才建議。
- **記憶體級距誤判**：分級門檻由 48/16/8 GB 改為 46/15/7.5 GB，
  修正 48 GB 機器因為可用量略低於 48 而被降級的問題。

### 新增

- `docs/benchmark-report.md`：掉速問題的完整排查實錄，
  含三次錯誤判斷的反省與「如何正確量測」的方法論。
- `tools/bench-ctx-load.ps1`：模擬長對話負載，量出速度與顯存曲線，
  用來找出顯示卡的顯存臨界點。
- 「本機最佳化」分頁新增**「適用前提」**說明，
  講明建議值是針對「文書電腦 + 後裝顯示卡」調校的。
- 標題列與視窗標題顯示版本號。
- README 新增專案定位聲明、ctx 實測對照表、顯存臨界點專章、
  以及 token 與檔案大小的換算參考。

### 調整

- 指令預覽面板加高並啟用自動換行，長指令不必捲動即可看完。
- 執行紀錄區預設高度提高，「本機最佳化」分頁收掉多餘留白。
- 多處提示文字改寫得更直白（`--no-mmap`、`--ctx-size`、`--cache-ram` 等），
  同步更新 `config/lang/zh-TW.sample.xml`。

### 實測結果

基準機（i5-9500 / 32 GB / RX 7900 XTX 24 GB / Qwen3.8-27B Q4_K_M）
套用新建議值後：

| 設定 | 修正前 | 修正後 |
| --- | --- | --- |
| `--ctx-size` | 65536 | 32768 |
| `--cache-ram` | 16384 | 4096 |
| `--no-mmap` | 預設開 | 預設關 |
| 長對話速度 | 9~10 t/s | 53~75 t/s |

## [1.2.0] - 2026-08

- 首次開源釋出。
- 視覺化設定 `llama-server` 的路徑、模型、推論與伺服器參數。
- 依 `--help` 實際輸出判斷可用旗標，llama.cpp 改版不會組出無效命令。
- Vulkan ICD 自動修復，多顯示卡環境可指定裝置。
- 設定檔可儲存、另存、刪除，支援多語系與自訂配色。
- 新增 GitHub Actions 自動建置與發布流程。

[1.3.0]: https://github.com/den13501/EasyLlama/releases/tag/v1.3.0
[1.2.0]: https://github.com/den13501/EasyLlama/releases/tag/v1.2.0
